{
  "_comment": "Multi-message fixture: sends TWO prompts in one session to reproduce bugs that only appear on the second turn (dedup races, stale awaitingAssistant state, parser picking up a previous `⏺` marker, feed duplication, etc). Don't change the prompts casually — the fixture's value is in being stable across runs so parser regressions are obvious.",
  "autoAcceptTrust": true,
  "steps": [
    { "type": "wait", "ms": 8000 },
    { "type": "send", "data": "say hi in three words\r" },
    { "type": "wait", "ms": 14000 },
    { "type": "send", "data": "now count from one to three\r" },
    { "type": "wait", "ms": 18000 }
  ]
}
