# Work-context recorded-shape census

Generated by `scripts/extract-work-context-fixtures.mts`. Do not hand-edit.

The cutoff is **2026-08-26T21:38:22.000Z**. The fixed cutoff matters because the measurement
process itself runs inside Codex and therefore appends new records; without it,
two consecutive extractions cannot be byte-identical.

No prompt, assistant prose, tool result, command body, raw path, session id, or
workspace title is included here. This is aggregate shape metadata only.

## Codex — full local corpus through cutoff

| Shape | Count |
|---|---:|
| files carrying relevant records | 1,672 |
| `session_meta` | 1,804 |
| session metadata with branch | 1,715 |
| session metadata without branch | 89 |
| `turn_context` | 15,064 |
| turn context with `workspace_roots` array | 4,560 |
| turn context without `workspace_roots` | 10,504 |
| `thread_settings_applied` | 2,838 |
| terminal `CommandExecution` | 5,796 |
| command cwd as `file://` URL | 5,796 |
| file-URL cwd with host / percent-encoding / malformed | 0 / 0 / 0 |
| command cwd as absolute path | 0 |
| command cwd missing/other | 0 |
| command represented as array | 5,796 |
| command represented as string | 0 |
| terminal `FileChange` | 830 |
| FileChange `changes` object | 830 |
| FileChange non-object shape | 0 |
| changed paths total / max per record | 1,111 / 8 |
| absolute / file-URL / relative changed paths | 1,111 / 0 / 0 |
| changed paths with / without a recognized checkout root | 1,080 / 31 |
| FileChange records spanning 2+ recognized checkout roots | 0 |
| completed `McpToolCall` | 1,372 |
| MCP calls whose arguments contain cwd | 18 |
| relevant records with no timestamp | 1 |

The recognized-checkout count is deliberately conservative: it recognizes a
path only when the recording preserves an explicit `.worktrees/<name>`
boundary or this machine's recorded Development project boundary. It does not
pretend that two arbitrary parent directories are two Git worktrees. Git still
owns final candidate validation in production.

### Terminal item status catalog

| CommandExecution status | Count |
|---|---:|
| `completed` | 5,339 |
| `failed` | 457 |

| FileChange status | Count |
|---|---:|
| `completed` | 830 |

### Completed item catalog

| Item discriminator | Count |
|---|---:|
| `Reasoning` | 11,720 |
| `CommandExecution` | 5,796 |
| `Extension` | 1,500 |
| `McpToolCall` | 1,372 |
| `AgentMessage` | 1,339 |
| `FileChange` | 830 |
| `SubAgentActivity` | 308 |
| `UserMessage` | 215 |
| `CollabAgentToolCall` | 192 |
| `ContextCompaction` | 102 |
| `ImageView` | 10 |

### Legacy Codex carriers already supported

| Carrier | Count |
|---|---:|
| `exec_command_end` | 26,872 |
| `exec_approval_request` | 0 |
| `local_shell_call` | 469 |
| `function_call` | 135,471 |

## Codex — 2026-08-26 through cutoff

| Shape | Count |
|---|---:|
| files carrying relevant records | 20 |
| `session_meta` | 17 |
| `turn_context` | 103 |
| `thread_settings_applied` | 51 |
| terminal `CommandExecution` | 2,442 |
| command cwd as `file://` URL | 2,442 |
| terminal `FileChange` / object-shaped | 326 / 326 |
| changed paths total / max per record | 470 / 8 |
| completed `McpToolCall` / with cwd argument | 488 / 8 |

## Claude — local corpus

| Shape | Count |
|---|---:|
| files carrying relevant records | 879 |
| explicit worktree enter | 11 |
| explicit worktree enter with branch | 11 |
| worktree enter from retained backup / current corpus | 11 / 0 |
| explicit worktree exit | 0 |
| first following conversation cwd at worktree / original / other | 4 / 0 / 0 |
| enters without a following conversation record | 7 |
| conversation entries with cwd | 234,822 |
| conversation entries with cwd + branch | 234,822 |
| agent-code conversation entries with cwd | 22,245 |

No recorded Claude exit exists in this corpus. Production supports the upstream
null-exit contract, but Stage 2 must not label an invented null record as a real
fixture. The explicit enters are retained provider recordings, but all 11 live
under a backup corpus rather than the current project corpus. Four have a next
conversation record; every one keeps the worktree cwd.

## Persisted Dispatch snapshot

| Observation | Value |
|---|---:|
| project tabs | 4 |
| visible Global Dispatch rows | 24 |
| rows whose visible and tab-local labels differ | 20 |
| recorded target visible label | `D23` |
| same target tab-local label | `D12` |

The reduced fixture is verified by the production row selector before it is
written. Its D23 mismatch is therefore a property of the recorded workspace
ordering, not a label typed into a test.

## Conclusions that constrain implementation

1. Current completed commands use `file://` cwd values; path matching must
   normalize that recorded representation once.
2. Every recorded FileChange uses an object keyed by changed path and has
   terminal status `completed`. There is no evidence for an array branch or
   a failed/declined fixture; those statuses come from the vendored upstream
   protocol and must fail closed rather than be represented by invented data.
3. Real MCP calls carry child/target cwd arguments, so recursive cwd mining is
   demonstrably unsafe.
4. Recorded Claude enter-to-conversation sequences preserve worktree cwd, so
   current Claude behavior can be protected without inventing precedence.
5. Provider recognition belongs in the shared work-context boundary because
   live and historical consumers receive the same recorded grammar.
