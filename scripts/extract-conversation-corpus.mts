// Replaced with the real extractor in the next commit (Task 2 of
// docs/superpowers/plans/2026-09-11-conversations.md). It exists now only so
// the tsconfig.node.json literal include resolves.
export {}
