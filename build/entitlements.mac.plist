<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron + node-pty native modules need JIT and dynamic native
       loading under hardened runtime. Keep these scoped to the desktop
       app target; helper tools remain external Homebrew binaries. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Dictation is opt-in, but hardened-runtime resource access is decided at
       signing time. Without this entitlement a correctly worded Info.plist
       prompt is not sufficient: Chromium can request the microphone while the
       signed process is still denied access underneath it. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
