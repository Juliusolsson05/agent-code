src.zip
node_modules
out
dist
.DS_Store
*.log
.vite
recordings/
# Real conversation JSONL dumps used as fixtures for perf/parser testing.
# These are multi-MB personal transcripts; scripts in testbench/ reference
# them by path, but the files themselves stay local.
testbench/fixtures/

# TypeScript incremental build cache
*.tsbuildinfo

# Reference material we read for understanding CC's internals — not part
# of cc-shell, intentionally left untracked. Live next to the project as
# a developer's local checkout.
claude-code-src/
